import bcrypt from 'bcrypt';
import DbClient from './db';
import jwt, { Secret, JwtPayload } from 'jsonwebtoken';
import { Feed } from './schema';


export default class FeedModel {
    static createFeed(name: string, description: string, )
}
